apiVersion: v1
kind: Pod
metadata:
  name: debug
spec:
  containers:
  - name: debug
    image: redis
    command: ["sh", "-c", "tail -f /dev/null"]
    resources:
      limits:
        memory: "8000Mi"
        cpu: "12000m"
      requests:
        memory: "4000Mi"
        cpu: "10000m"
