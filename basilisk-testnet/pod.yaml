apiVersion: v1
kind: Pod
metadata:
  name: redis
spec:
  containers:
  - name: redis
    image: redis
    volumeMounts:
    - name: persistent-storage
      mountPath: /tmp
  volumes:
  - name: persistent-storage
    persistentVolumeClaim:
      claimName: efs-pv